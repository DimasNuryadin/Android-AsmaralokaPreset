<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asmaralokapreset">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsmaralokaPreset">
        <activity android:name=".PembayaranBerhasil" />
        <activity android:name=".PembayaranActivity" />
        <activity android:name=".Pack" />
        <activity android:name=".MoodyFall" />
        <activity android:name=".ActivityUtama"></activity>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityRegister" />
        <activity android:name=".ActivityLogin" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>